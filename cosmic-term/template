# Template file for 'cosmic-term'
pkgname=cosmic-term
version=1.0.14
revision=1
build_style=cargo
_epoch_tag="epoch-${version}"
distfiles="https://github.com/pop-os/cosmic-term/archive/refs/tags/${_epoch_tag}.tar.gz"
checksum="<sha256sum-here>"  # Run: xgensum -f srcpkgs/cosmic-term/template

short_desc="COSMIC terminal emulator"
maintainer="Maintainer <maintainer@example.com>"
license="GPL-3.0-or-later"
homepage="https://github.com/pop-os/cosmic-term"

hostmakedepends="pkg-config cargo rust"
makedepends="
 wayland-devel
 libxkbcommon-devel
 freetype-devel
 fontconfig-devel"

do_install() {
	vbin target/release/cosmic-term
	vmkdir usr/share/applications
	vinstall data/cosmic-term.desktop 644 usr/share/applications
	vlicense LICENSE
}
